stages:
    # - build
    - docker_build

cache:
    paths:
        - .m2/repository

# build:
#     stage: build
# image: maven:3.6.3-jdk-11
#     script:
#         - mvn package -DskipTests
#     artifacts:
#         paths:
#             - target/*.war
#     tags:
#         - rapla

docker_build:
    stage: docker_build
    image: docker:24.0.5
    services:
        - docker:24.0.5-dind
    script:
        - docker image build -t rapla .
    tags:
        - rapla
